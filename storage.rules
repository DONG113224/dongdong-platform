rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 課程檔案：登入用戶可寫入（前端已有 AdminGuard 保護），一般用戶禁止直接讀取
    match /course-files/{courseId}/{allPaths=**} {
      allow read: if false;
      allow write: if request.auth != null;
    }
  }
}
