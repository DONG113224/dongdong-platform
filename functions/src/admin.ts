// Admin API endpoints
// Currently admin operations (refund, order listing) are handled
// through Firestore direct reads (with security rules) and the refund endpoint.
// Additional admin-only endpoints can be added here as needed.

export {};
